title: "F8fq"
time: "2026/09/22"
text: "const KEY = new TextEncoder().encode("原神启动");

const 原神牛逼加密 = s => btoa(String.fromCharCode(
  ...new TextEncoder().encode(s).map((b, i) => b ^ KEY[i % KEY.length])
));

const 原神牛逼解密 = s => new TextDecoder().decode(
  Uint8Array.from(atob(s), c => c.charCodeAt(0))
    .map((b, i) => b ^ KEY[i % KEY.length])
);

console.log(原神牛逼加密("原神牛逼")); // AAAAAAAAAhk0DAoU
console.log(原神牛逼解密("AAAAAAAAAhk0DAoU")); // 原神牛逼"